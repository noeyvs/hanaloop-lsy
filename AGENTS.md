# AGENTS.md

## 📌 기본 작업 원칙

- 사용자가 요청한 태스크 또는 피처 개발은 독립적인 작업 단위로 진행한다.
- 하나의 피처는 하나의 브랜치에서 개발한다.
- 작업 중 불필요한 변경, 포맷팅, 리팩터링은 요청 범위를 벗어나면 수행하지 않는다.
- 기존 코드 스타일, 폴더 구조, 네이밍 규칙을 우선적으로 따른다.

## 🌿 브랜치 전략

- 새 피처 또는 태스크를 시작할 때는 전용 브랜치를 생성한다.
- 브랜치명은 다음 형식을 따른다.

```bash
feature/<short-description>
fix/<short-description>
chore/<short-description>
docs/<short-description>
```

## 💾 커밋 전략

- 작업 중간 커밋은 자유롭게 생성할 수 있다.
- 최종적으로 원격 저장소에 푸쉬하기 전에는 반드시 **스쿼시 커밋**으로 정리한다.
- 하나의 피처 또는 태스크 완료 시 최종 커밋은 **1개만** 남긴다.
- 커밋 메시지는 작업 내용을 명확히 설명해야 한다.

**예시:**

```bash
feat: add user login feature
fix: resolve payment validation error
docs: summarize user login implementation
```

## 🚀 푸쉬 규칙

- 하나의 피처 개발이 완료되면 해당 피처 브랜치로 푸쉬한다.
- `main`, `master`, `develop` 브랜치에 직접 푸쉬하지 않는다.
- 푸쉬 전 다음을 확인한다.

  ```bash
  git status
  git log --oneline
  ```

- 커밋이 여러 개라면 interactive rebase 등을 사용해 하나로 스쿼시한다.

  ```bash
  git rebase -i HEAD~<commit-count>
  ```

- 이후 피처 브랜치로 푸쉬한다.

  ```bash
  git push origin <branch-name>
  ```

## 📝 문서화 규칙

- 피처 또는 태스크 완료 후 수행한 내용을 문서화한다.
- 문서화 위치는 다음 중 하나를 따른다.
  - 기존 `README.md`에 해당 내용 추가
  - 피처별 문서 파일 생성

**예시:**

```text
docs/user-login.md
docs/payment-validation.md
features/user-login.md
```

### 📄 문서에 포함할 내용

피처별 문서 또는 README에는 다음 내용을 정리한다.

> # <Feature Name>
>
> ## 개요
>
> 이 피처에서 구현한 내용을 간단히 설명한다.
>
> ## 변경 사항
>
> - 추가된 기능
> - 수정된 로직
> - 변경된 파일 또는 주요 모듈
> - 제거된 코드가 있다면 그 이유
>
> ## 사용 방법
>
> 필요한 경우 실행 방법, API 사용법, UI 접근 방법 등을 작성한다.
>
> ## 테스트
>
> 수행한 테스트 내용을 작성한다.
> (예시: unit test 통과, integration test 통과, 수동 테스트 완료, 특정 케이스 검증 완료 등)
>
> ## 참고 사항
>
> 리뷰어 또는 이후 작업자가 알아야 할 내용을 작성한다.

## ✅ 작업 완료 전 체크리스트

작업 완료 전 반드시 다음을 확인한다.

- [ ] 요청 범위 내에서만 변경했는가?
- [ ] 불필요한 파일 변경이 없는가?
- [ ] 테스트 또는 빌드가 가능한 경우 실행했는가?
- [ ] 커밋이 하나로 스쿼시되었는가?
- [ ] 피처 브랜치로 푸쉬했는가?
- [ ] README 또는 피처 문서에 수행 내용을 정리했는가?

## 🚫 금지 사항

- `main`, `master`, `develop` 브랜치에 직접 푸쉬하지 않는다.
- 여러 개의 커밋을 그대로 원격 브랜치에 푸쉬하지 않는다.
- 문서화 없이 피처 개발을 종료하지 않는다.
- 사용자 요청과 무관한 대규모 리팩터링을 수행하지 않는다.
- 민감 정보, API 키, 비밀번호, 토큰을 커밋하지 않는다.
